apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: boutique
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      automountServiceAccountToken: false
      containers:
        - name: order-service
          image: 123209654419.dkr.ecr.us-east-1.amazonaws.com/order-service:6070b4fdb4c98fe92df77f37683c82ab6c8a758f
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3004
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3004"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: boutique-secrets
                  key: ORDERS_DB_URL
            - name: JWT_ACCESS_SECRET
              valueFrom:
                secretKeyRef:
                  name: boutique-secrets
                  key: JWT_ACCESS_SECRET
          envFrom:
            - configMapRef:
                name: boutique-config
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: tmp
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: boutique
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  # BUGFIX: this Service previously declared port/targetPort 3002 while the
  # container listens on 3004, so it could never route traffic. Note that the
  # gateway still does not proxy to this service at all (it reads
  # ORDERS_SERVICE_URL, not ORDER_SERVICE_URL) — see SECURITY-REMEDIATION.md.
  ports:
    - name: http
      protocol: TCP
      port: 3004
      targetPort: http
  type: ClusterIP
